name: kaido_ui
description: Shared UI (theme, router, app shell) for the Kaido mobile apps.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  go_router: ^17.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.3.0
