import 'package:flutter/material.dart';

/// Contact location picker screen (`/contact/map`).
class ContactMapPage extends StatelessWidget {
  /// Creates a [ContactMapPage].
  const ContactMapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('位置選択')),
      body: const Center(child: Text('TODO: Phase 4B/4C')),
    );
  }
}
