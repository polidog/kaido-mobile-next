name: kaido_mobile_next
publish_to: none
version: 0.0.0

environment:
  sdk: ^3.12.0

workspace:
  - apps/tokaido
  - apps/nakasendo
  - apps/koshu
  - apps/nikko
  - apps/oshu
  - packages/kaido_api
  - packages/kaido_data
  - packages/kaido_ui

dev_dependencies:
  melos: ^8.2.0
  very_good_analysis: ^10.3.0

melos:
  name: kaido_mobile

  scripts:
    analyze:
      description: Run dart analyze in all packages
      run: melos exec -- dart analyze .

    test:
      description: Run tests in all packages
      run: melos exec -- flutter test

    build:gen:
      description: Run code generation (freezed, riverpod, retrofit)
      run: melos exec -- dart run build_runner build --delete-conflicting-outputs
      packageFilters:
        dependsOn: build_runner

    clean:
      description: Clean all packages
      run: melos exec -- flutter clean
